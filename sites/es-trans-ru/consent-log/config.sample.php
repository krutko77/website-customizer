<?php
/**
 * Доступы для sendmail.php — es-trans.ru
 *
 * ============================================================
 * КУДА КЛАСТЬ: ВНЕ public_html
 * ============================================================
 * На Timeweb структура обычно такая:
 *
 *   /home/u12345/es-trans.ru/
 *       public_html/            <- сюда смотрит веб-сервер, это САЙТ
 *           files/sendmail/sendmail.php
 *       config/                 <- сюда положить этот файл
 *           es-trans-config.php
 *
 * Файл ОБЯЗАН лежать на уровень выше public_html. Если положить его внутрь
 * сайта, его можно будет скачать по прямой ссылке вместе со всеми паролями.
 *
 * Права на файл: 600 (chmod 600) — читать может только владелец.
 *
 * Переименовать в es-trans-config.php и заполнить реальными значениями.
 * ЭТОТ ФАЙЛ (config.sample.php) — образец, в нём не должно быть настоящих
 * паролей, он лежит в git.
 */

return [

    // ---------- Почта (SMTP Timeweb) ----------
    'smtp' => [
        'host'     => 'smtp.timeweb.ru',
        'port'     => 465,
        'user'     => 'main@es-trans.ru',

        // ВНИМАНИЕ: старый пароль 'es-trans#2025' лежал открытым текстом
        // в sendmail.php в веб-доступной директории и был передан в переписке.
        // Он скомпрометирован — смените его в панели Timeweb и впишите новый
        // СЮДА, а не в код сайта.
        'password' => 'ВПИШИТЕ_НОВЫЙ_ПАРОЛЬ',

        'from'      => 'main@es-trans.ru',
        'from_name' => 'Сайт ЕС Транс',
        'to'        => 'kpv@es-trans.pro',
    ],

    // ---------- База данных (журнал согласий) ----------
    // Реквизиты берутся в панели Timeweb → Базы данных.
    'db' => [
        'host'     => 'localhost',
        'name'     => 'ВПИШИТЕ_ИМЯ_БД',
        'user'     => 'ВПИШИТЕ_ПОЛЬЗОВАТЕЛЯ',
        'password' => 'ВПИШИТЕ_ПАРОЛЬ_БД',
        'charset'  => 'utf8mb4',
    ],

    // ---------- Журнал согласий ----------
    'consent' => [
        // Версия текста согласия. МЕНЯТЬ при каждой правке формулировки
        // в форме или в Политике/Соглашении. Иначе нельзя будет доказать,
        // какую именно редакцию человек видел.
        'version' => '2026-08-18',

        // Текст, который реально показан рядом с чекбоксом.
        // Должен совпадать с версткой формы дословно.
        'text' => 'Я даю согласие на обработку моих персональных данных в соответствии с Политикой конфиденциальности',

        // Резервный файл на случай, если БД недоступна. Тоже ВНЕ public_html.
        // Согласие не должно потеряться из-за сбоя базы.
        'fallback_log' => __DIR__ . '/consents-fallback.log',
    ],
];
