<?php
/**
 * ДОБАВКА к es-trans-config.php — настройки защиты от ботов.
 *
 * Это НЕ отдельный файл конфига. Скопируйте блок 'rate_limit' в
 * существующий /config/es-trans-config.php — внутрь массива, который он
 * возвращает, рядом с 'smtp', 'db' и 'consent'.
 *
 * Блок НЕОБЯЗАТЕЛЬНЫЙ: без него обработчик возьмёт значения по умолчанию
 * (5 заявок за 600 секунд, каталог /config/rate-limit). Добавляйте, только
 * если нужно изменить лимит или путь.
 */

return [

    // ... существующие 'smtp', 'db', 'consent' остаются как есть ...

    // ---------- Ограничение частоты отправки ----------
    'rate_limit' => [

        // Каталог для счётчиков. ОБЯЗАТЕЛЬНО вне public_html.
        // Создать вручную с правами 700:
        //     mkdir -p /config/rate-limit && chmod 700 /config/rate-limit
        //
        // Если каталога нет или он недоступен для записи — ограничение
        // просто отключается, приём заявок продолжает работать.
        'dir' => __DIR__ . '/rate-limit',

        // Сколько заявок с одного IP разрешено за окно.
        // 5 — с запасом: человеку хватает 1–2 даже при отправке из
        // нескольких форм подряд.
        // Поставить 0, чтобы выключить ограничение совсем.
        'max_per_window' => 5,

        // Длина окна в секундах. 600 = 10 минут.
        'window_seconds' => 600,
    ],
];
