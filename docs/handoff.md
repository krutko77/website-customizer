# 🤝 ПЕРЕДАЧА ПРОЕКТА — ЧИТАЙ ЭТО ПЕРВЫМ

> Этот файл — точка входа для **новой нейронки** (или человека), которая
> впервые открывает проект. Цель: за 5 минут понять **что это, где мы
> сейчас и куда идём** — без необходимости читать весь код.

---

## Как устроена память проекта

Весь контекст проекта живёт в четырёх файлах. У каждого — одна роль.
Читай их **в этом порядке**:

| # | Файл | Отвечает на вопрос | Как меняется |
|---|------|--------------------|--------------|
| 1 | [project-brief.md](./project-brief.md) | **Что** это за проект и **зачем** | Редко (меняется суть проекта) |
| 2 | [state.md](./state.md) | **Где мы сейчас** и **что делать дальше** | Постоянно — это «живой снимок» |
| 3 | [changelog.md](./changelog.md) | **Что уже сделали** (по датам) | Только дописывается вниз |
| 4 | [decisions.md](./decisions.md) | **Почему** так, а не иначе | Только дописывается вниз |

Плюс [../CLAUDE.md](../CLAUDE.md) — инструкции по стеку, деплою и правилам работы.

**Правило, которое держит всё это в актуальном состоянии:**
после каждого осмысленного шага мы обновляем `state.md` + дописываем
`changelog.md` и **коммитим в git**. Поэтому история проекта = история
коммитов, а текущее положение дел всегда в `state.md`. Подробнее — в
разделе «ЖУРНАЛ ПРОЕКТА И КОММИТЫ» в `CLAUDE.md`.

---

## Быстрый старт для входящей нейронки

1. Прочитай 4 файла выше по порядку.
2. Глянь последние коммиты — это лента того, что реально делалось:
   ```bash
   git log --oneline -20
   ```
3. Если нужно увидеть, что менялось в конкретном шаге:
   ```bash
   git show <hash>
   ```
4. Посмотри, какие скиллы доступны — это основной инструментарий проекта:
   ```bash
   ls .claude/skills/
   ```
   Описание каждого — в [architecture.md](./architecture.md).
5. Возьми первый пункт из раздела «Следующие шаги» в `state.md` — и продолжай.

---

## Снимок передачи

> Перед тем как передать проект дальше, обнови блок ниже (или вызови `/handoff`).
> Это TL;DR на одну страницу — самое важное, что нельзя потерять.

**Дата снимка:** 2026-08-11
**В одном предложении:** Подготовлен и запушен на GitHub cookie-consent баннер для первого реального сайта в работе (es-trans.ru, клиент ЕС Транс), протестирован в headless Playwright; GitHub PAT ротирован, upstream-трекинг репозитория полностью исправлен.
**Что работает прямо сейчас:** 35 скиллов в `.claude/skills/` — правка вёрстки, дизайн и темы (`frontend-design`, `theme-factory`, `ui-ux-pro-max`) и процесс (`superpowers`, `/log`, `/end`). Браузерная автоматизация (`playwright-skill`, `webapp-testing`) проверена и использована для реальной задачи. `sites/es-trans-ru/cookie-banner/` содержит готовый и протестированный (6/6 сценариев) cookie-баннер с инструкцией по внедрению (`README.md`) — заменяет сторонний скрипт, который не блокировал Яндекс.Метрику до согласия пользователя. Репозиторий на GitHub: `github.com/krutko77/website-customizer`, ветка `main` запушена, upstream-трекинг `main` → `origin/main` работает штатно.
**Что в процессе / недоделано:** Cookie-баннер не выложен на прод es-trans.ru — нет доступа к деплою/CMS клиента, ждём доступов или передачи README.md исполнителю. `project-brief.md` заполнен частично (клиент/сайт указаны, доступы/дизайн — ещё нет). Стек не выбран — `package.json` без зависимостей, серверной части нет.
**Где живёт прод (URL / SERVER_ID):** Не задеплоен (в рамках этого репозитория). Целевой сайт клиента — https://es-trans.ru/ (правки туда пока не выложены). VPS: 201.51.4.183, следующий свободный порт был 3004 (сверить перед использованием). `deploy-vps.sh` применим только если появится серверная часть — сейчас он ссылается на несуществующий `src/server.js`.
**Главные подводные камни:** `.env.example` НЕ в `.gitignore` (в отличие от `.env`) — секреты туда класть нельзя, уйдут в публичный репозиторий. Старый GitHub PAT `ghp_...ye1EkTdP` заменён новым (`ghp_...uV2H91Vj`) 2026-08-11 — пользователь должен подтвердить, что старый реально отозван на GitHub. Новый токен нигде не сохранён в репозитории — при следующем пуше без настроенного credential helper снова понадобится токен от пользователя. Прямые fetch к github.com заблокированы — работать через `curl`/`git` (обычные внешние сайты через `curl` доступны без ограничений). Запись в `.claude/commands/` только через python3.
**Первое, что стоит сделать новому исполнителю:** Подтвердить отзыв старого GitHub PAT, затем получить доступ к файлам es-trans.ru и выложить cookie-баннер по инструкции `sites/es-trans-ru/cookie-banner/README.md`.
