# 🤝 ПЕРЕДАЧА ПРОЕКТА — ЧИТАЙ ЭТО ПЕРВЫМ

> Этот файл — точка входа для **новой нейронки** (или человека), которая
> впервые открывает проект. Цель: за 5 минут понять **что это, где мы
> сейчас и куда идём** — без необходимости читать весь код.

---

## Как устроена память проекта

Весь контекст проекта живёт в четырёх файлах. У каждого — одна роль.
Читай их **в этом порядке**:

| # | Файл | Отвечает на вопрос | Как меняется |
|---|------|--------------------|--------------|
| 1 | [project-brief.md](./project-brief.md) | **Что** это за проект и **зачем** | Редко (меняется суть проекта) |
| 2 | [state.md](./state.md) | **Где мы сейчас** и **что делать дальше** | Постоянно — это «живой снимок» |
| 3 | [changelog.md](./changelog.md) | **Что уже сделали** (по датам) | Только дописывается вниз |
| 4 | [decisions.md](./decisions.md) | **Почему** так, а не иначе | Только дописывается вниз |

Плюс [../CLAUDE.md](../CLAUDE.md) — инструкции по стеку, деплою и правилам работы.

**Правило, которое держит всё это в актуальном состоянии:**
после каждого осмысленного шага мы обновляем `state.md` + дописываем
`changelog.md` и **коммитим в git**. Поэтому история проекта = история
коммитов, а текущее положение дел всегда в `state.md`. Подробнее — в
разделе «ЖУРНАЛ ПРОЕКТА И КОММИТЫ» в `CLAUDE.md`.

---

## Быстрый старт для входящей нейронки

1. Прочитай 4 файла выше по порядку.
2. Глянь последние коммиты — это лента того, что реально делалось:
   ```bash
   git log --oneline -20
   ```
3. Если нужно увидеть, что менялось в конкретном шаге:
   ```bash
   git show <hash>
   ```
4. Посмотри, какие скиллы доступны — это основной инструментарий проекта:
   ```bash
   ls .claude/skills/
   ```
   Описание каждого — в [architecture.md](./architecture.md).
5. Возьми первый пункт из раздела «Следующие шаги» в `state.md` — и продолжай.

---

## Снимок передачи

> Перед тем как передать проект дальше, обнови блок ниже (или вызови `/handoff`).
> Это TL;DR на одну страницу — самое важное, что нельзя потерять.

**Дата снимка:** 2026-08-11
**В одном предложении:** Клиент выложил cookie-баннер для es-trans.ru на прод самостоятельно, но не полностью — баннер не появляется; причина найдена (не подключены файлы `cookie-banner.js`/`.css` в HTML и не залиты на сервер) и объяснена пользователю, точные патчи под актуальный HTML ещё предстоит подготовить.
**Что работает прямо сейчас:** 35 скиллов в `.claude/skills/` — правка вёрстки, дизайн и темы (`frontend-design`, `theme-factory`, `ui-ux-pro-max`) и процесс (`superpowers`, `/log`, `/end`). Браузерная автоматизация (`playwright-skill`, `webapp-testing`) проверена и использована для реальной задачи. `sites/es-trans-ru/cookie-banner/` содержит готовый и протестированный локально (6/6 сценариев) cookie-баннер с инструкцией по внедрению (`README.md`). На проде (2026-08-11) применена только часть инструкции: обёртка Яндекс.Метрики в `window.__esTransInitMetrika` — есть и корректна; сам баннер (файлы + подключение в HTML) — нет. Репозиторий на GitHub: `github.com/krutko77/website-customizer`, ветка `main`, upstream-трекинг работает штатно.
**Что в процессе / недоделано:** Прод es-trans.ru сейчас временно вообще без cookie-баннера (старый сторонний скрипт `cookieinfoscript.com` клиент уже удалил, новый ещё не подключён) — `js/cookie-banner.js` и `css/cookie-banner.css` отдают 404, тега подключения скрипта в HTML нет. Нужно подготовить точные патчи под актуальный HTML (`app.min.js?_v=20260811171125` на момент снимка, сверить заново) и передать клиенту. `project-brief.md` заполнен частично. Стек не выбран — `package.json` без зависимостей, серверной части нет.
**Где живёт прод (URL / SERVER_ID):** Не задеплоен (в рамках этого репозитория). Целевой сайт клиента — https://es-trans.ru/ (клиент выкладывает правки сам, у нас нет прямого доступа к серверу сайта). VPS: 201.51.4.183, следующий свободный порт был 3004 (сверить перед использованием). `deploy-vps.sh` применим только если появится серверная часть — сейчас он ссылается на несуществующий `src/server.js`.
**Главные подводные камни:** Сайт временно без cookie-баннера — закрыть быстро. `.env.example` НЕ в `.gitignore` (в отличие от `.env`) — секреты туда класть нельзя. Старый GitHub PAT `ghp_...ye1EkTdP` заменён новым — пользователь должен подтвердить отзыв старого на GitHub. Новый токен нигде не сохранён в репозитории. Прямые fetch к github.com заблокированы — работать через `curl`/`git` (обычные внешние сайты, включая es-trans.ru, через `curl` доступны без ограничений). Запись в `.claude/commands/` только через python3.
**Первое, что стоит сделать новому исполнителю:** Скачать актуальный HTML `curl https://es-trans.ru/`, подготовить точные строки-патчи (подключение `css/cookie-banner.css` в `<head>` и `js/cookie-banner.js` перед `</body>`) и передать клиенту вместе с напоминанием залить сами файлы на сервер.
