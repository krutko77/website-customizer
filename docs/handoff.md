# 🤝 ПЕРЕДАЧА ПРОЕКТА — ЧИТАЙ ЭТО ПЕРВЫМ

> Этот файл — точка входа для **новой нейронки** (или человека), которая
> впервые открывает проект. Цель: за 5 минут понять **что это, где мы
> сейчас и куда идём** — без необходимости читать весь код.

---

## Как устроена память проекта

Весь контекст проекта живёт в четырёх файлах. У каждого — одна роль.
Читай их **в этом порядке**:

| # | Файл | Отвечает на вопрос | Как меняется |
|---|------|--------------------|--------------|
| 1 | [project-brief.md](./project-brief.md) | **Что** это за проект и **зачем** | Редко (меняется суть проекта) |
| 2 | [state.md](./state.md) | **Где мы сейчас** и **что делать дальше** | Постоянно — это «живой снимок» |
| 3 | [changelog.md](./changelog.md) | **Что уже сделали** (по датам) | Только дописывается вниз |
| 4 | [decisions.md](./decisions.md) | **Почему** так, а не иначе | Только дописывается вниз |

Плюс [../CLAUDE.md](../CLAUDE.md) — инструкции по стеку, деплою и правилам работы.

**Правило, которое держит всё это в актуальном состоянии:**
после каждого осмысленного шага мы обновляем `state.md` + дописываем
`changelog.md` и **коммитим в git**. Поэтому история проекта = история
коммитов, а текущее положение дел всегда в `state.md`. Подробнее — в
разделе «ЖУРНАЛ ПРОЕКТА И КОММИТЫ» в `CLAUDE.md`.

---

## Быстрый старт для входящей нейронки

1. Прочитай 4 файла выше по порядку.
2. Глянь последние коммиты — это лента того, что реально делалось:
   ```bash
   git log --oneline -20
   ```
3. Если нужно увидеть, что менялось в конкретном шаге:
   ```bash
   git show <hash>
   ```
4. Посмотри, какие скиллы доступны — это основной инструментарий проекта:
   ```bash
   ls .claude/skills/
   ```
   Описание каждого — в [architecture.md](./architecture.md).
5. Возьми первый пункт из раздела «Следующие шаги» в `state.md` — и продолжай.

---

## Снимок передачи

> Перед тем как передать проект дальше, обнови блок ниже (или вызови `/handoff`).
> Это TL;DR на одну страницу — самое важное, что нельзя потерять.

**Дата снимка:** 2026-08-18 (конец сессии, повторный аудит)
**В одном предложении:** На проде es-trans.ru закрыт гейтинг Яндекс.Карт и отложенная загрузка Битрикс24 плюс найденный в этой же сессии дефект определения языка модалок (63/63 проверки зелёные), сверен фактический набор cookie с перечнем в Политике (владелец сразу же поправил Политику, сверено), а главная находка — согласия из трёх не-битриксовых форм **не сохранялись нигде** — закрыта готовым, но пока не выложенным патчем `sites/es-trans-ru/consent-log/`; самое срочное сейчас не код, а смена скомпрометированного пароля почты. **Итог вечернего повторного аудита 18.08 (`docs/checklists/audit-es-trans-ru-2026-08-18.md`):** всё выложенное 17–18.08 подтверждено на живом проде (cookie-блок эталонный — 12 страниц × 3 сценария, до выбора и при отказе 0 внешних хостов и 0 cookie везде; разметка форм чиста на 9 страницах; почта на своём домене; перечень действий в Согласии полный; язык согласия совпадает с языком страницы), а незакрытых технических замечаний осталось три — **нет серверной проверки согласия** (🔴 ч. 1 ст. 9: прямой POST на `sendmail.php` без поля `agreement` принимается неотличимо от запроса с ним — 200/0 байт/0.068 с, как и на пустой POST и GET; через браузер блокировка работает, но обходится отключением JS), скомпрометированный пароль SMTP (🔴 ч. 1 ст. 19) и перечень ПДн в документах шире реально собираемого формами (🟡 ст. 5); первые два закрываются одной выкладкой уже готового патча `consent-log/`, начиная со смены пароля.
**Что работает прямо сейчас:** 35 скиллов в `.claude/skills/` — правка вёрстки, дизайн и темы (`frontend-design`, `theme-factory`, `ui-ux-pro-max`) и процесс (`superpowers`, `/log`, `/end`). Браузерная автоматизация (`playwright-skill`, `webapp-testing`) проверена и многократно использована на живом проде. На `https://es-trans.ru/` работает end-to-end: cookie-баннер (до выбора и при отказе — **0 cookie и 0 внешних хостов**, подтверждено 2026-08-18 на 12 страницах), гейтинг Яндекс.Метрики, **Яндекс.Карт** (заглушка с кнопкой «Показать карту» вместо iframe) и **отложенная загрузка формы Битрикс24**, точка отзыва согласия «Настройки cookie» в футере, локализация всех наших модалок по цепочке `localStorage['language'] || navigator.language.slice(0,2) || 'ru'` со списком `['ru','en','cn']` — как у самого сайта. Прод-проверка `consent-gating-fix/verify.cjs`: **63/63**, отдельно модалка мессенджера 9/9 по локалям. Согласие во всех трёх формах отображается корректно на всех страницах. Есть переиспользуемый чек-лист аудита по 152-ФЗ ред. 2026 — `docs/checklists/152-fz-website-audit.md` (🔍 проверяется технически / 🧑‍⚖️ нужен ответ владельца), примеры применения — `audit-es-trans-ru-2026-08-17.md` и `audit-cookie-vs-policy-2026-08-18.md`. Универсальный шаблон баннера — `sites/cookie-banner-universal/`. Репозиторий на GitHub: `github.com/krutko77/website-customizer`, ветка `main`.
**Что в процессе / недоделано:** 🔴 **Первое и срочное — не код:** пароль от `main@es-trans.ru` лежал открытым текстом в `sendmail.php` в веб-доступной директории и был передан в переписку, то есть **скомпрометирован** (ч. 1 ст. 19 152-ФЗ) — сменить в панели Timeweb, новый вписывать только в конфиг **вне** `public_html` (chmod 600) и убедиться, что `https://es-trans.ru/config/es-trans-config.php` отдаёт 404. **Готов, но не выложен:** `sites/es-trans-ru/consent-log/` — журнал согласий в таблице MySQL для форм `form-customs`/`form-offer`/`form-driver` (для формы на главной согласия хранит Битрикс24). Кроме собственно журнала патч чинит пять дефектов `sendmail.php`: пароль в коде, отсутствие серверной проверки согласия (обходилась выключением JS), отключённый антибот (заменён на honeypot), XSS в письме, всегда-истинные условия `trim(!empty(...))`. Локально 8/8 на PHP 8.2 + проверен фолбэк в файл при недоступной БД; **на живой MySQL не прогонялся** — в этой среде нет драйверов PDO. Порядок выкладки строго по README патча (пароль → `schema.sql` → конфиг вне `public_html` + проверка 404 → honeypot + полная пересборка Gulp → `sendmail.php` → тестовые заявки). **Два старых патча без обратной связи с 2026-08-13:** `sites/es-trans-ru/messenger-consent/` (базовая версия; фикс языка из этой сессии уже на проде) и `sites/es-trans-ru/max-icon-fix/`. **Организационный хвост по 152-ФЗ (кода не требует):** договор поручения на обработку (ч. 3 ст. 6) с Яндексом, Битрикс24 и Timeweb + сверка их реквизитов по свежей выписке ЕГРЮЛ; уведомление в РКН и запись в реестре операторов; регламент реагирования на утечку (ст. 21); ответственный за обработку ПДн; рабочий ли ящик `info@es-trans.pro` (чужой домен `.pro`, а это канал отзыва согласия); перечень ПДн в Политике шире реально собираемого формами (ст. 5). Общие хвосты: `project-brief.md` заполнен частично, стек не выбран — `package.json` без зависимостей, серверной части нет, отзыв старого GitHub PAT `ghp_...ye1EkTdP` не подтверждён.
**Где живёт прод (URL / SERVER_ID):** Не задеплоен (в рамках этого репозитория). Целевой сайт клиента — https://es-trans.ru/ (клиент сам держит исходники в отдельном Gulp-проекте на своём ноутбуке, вне доступа этой среды, и сам выкладывает правки — у нас нет прямого доступа ни к файлам, ни к серверу). VPS: 201.51.4.183, следующий свободный порт был 3004 (сверить перед использованием). `deploy-vps.sh` применим только если появится серверная часть — сейчас он ссылается на несуществующий `src/server.js`.
**Главные подводные камни:** **Отдельного `js/cookie-banner.js` на проде НЕТ** — баннер минифицирован внутрь `js/app.min.js`, README ранних патчей (`cookie-banner/`, `cookie-banner-i18n-fix/`) утверждают обратное и это неверно; актуальный исходник, совпадающий с продом, — `sites/es-trans-ru/consent-gating-fix/cookie-banner.js`, а `sites/es-trans-ru/cookie-banner/cookie-banner.js` помечен устаревшим. После каждой выкладки проверять, что в бандле **одна** копия строки `es-trans-cookie-consent`, иначе на сайте будет два баннера. **Попапы с формами и футер продублированы в разметке КАЖДОЙ страницы es-trans.ru** — общего include нет, поэтому правка одного шаблона без полной пересборки расходится не на весь сайт (именно так первая выкладка правки форм 2026-08-17 попала только на одну страницу из шести). При правках форм/футера проверять все страницы: сравнить `last-modified` и искомый фрагмент разметки по списку. **Любой динамически созданный элемент на этом сайте не переводится** — функция `je()` в `js/app.min.js` отрабатывает один раз при загрузке страницы и при смене `#languageSelect`, а элемент создаётся позже; наступили уже трижды (messenger-consent, cookie-баннер), лечение всегда одно — свой словарь `TRANSLATIONS` + чтение `localStorage['language']`. **Канонические файлы в `sites/es-trans-ru/` могут отставать от прода** (так и обнаружился `cookie-banner/cookie-banner.js`) — перед правкой сверяться с реальным `app.min.js` с прода, а не доверять репозиторию; `grep` по нему отваливается по таймауту, куски тянуть точечно через python3. Формы живут в скрытых попапах — Playwright не кликнет напрямую («outside of the viewport»), нужно через `page.evaluate` форсировать `display/visibility/opacity` у родителей, иначе получаются ложные ❌. Корень проекта в этой среде — `/home/my_workspace/website-customizer` (раньше был `/data/vibe-students/krutko77/website-customizer`; захардкоженные пути в `.claude/settings.json` уже поправлены 2026-08-17, но могут всплыть ещё где-то в конфигах). Реальные файлы сайта клиента недоступны для прямого редактирования — все правки готовятся как точный текстовый патч в чате и передаются клиенту, который сам вносит их и пересобирает через Gulp; канонические файлы-эталоны в `sites/es-trans-ru/` (`cookie-banner/`, `bitrix24-form/`) нужно вручную синхронизировать с тем, что реально ушло клиенту — и это ОТДЕЛЬНАЯ копия логики от `sites/cookie-banner-universal/` (шаблон для будущих сайтов), они не связаны импортами. При любом гейтинге трекера — оборачивать нужно ВСЮ загрузку скрипта-счётчика, не только вызов инициализации (см. принцип №1 в `sites/cookie-banner-universal/README.md`) — на этом уже дважды спотыкались в этом проекте. Эта VS Code-сессия подключена через локальный shim-прокси (`ANTHROPIC_BASE_URL=http://172.30.0.1:8190`), не напрямую к аккаунту claude.ai — `/status`/Usage не показывают реальные лимиты подписки. `.env.example` НЕ в `.gitignore` (в отличие от `.env`) — секреты туда класть нельзя. Старый GitHub PAT `ghp_...ye1EkTdP` заменён новым — пользователь должен подтвердить отзыв старого на GitHub. Прямые fetch к github.com заблокированы — работать через `curl`/`git` (обычные внешние сайты, включая es-trans.ru, через `curl` доступны без ограничений). Запись в `.claude/commands/` только через python3.
**Первое, что стоит сделать новому исполнителю:** Спросить, **сменил ли пользователь пароль** от `main@es-trans.ru` в панели Timeweb — это самый срочный пункт, он важнее любого кода. Затем спросить, выложен ли патч `sites/es-trans-ru/consent-log/`; если да — прогнать проверку всех трёх форм (`form-customs`, `form-offer`, `form-driver`): письмо приходит и содержит блок про согласие, запись появилась в таблице `consents`, honeypot добавлен на всех страницах, конфиг **не** открывается по прямой ссылке. Если ещё нет — пройти с пользователем README патча по шагам. Заодно спросить про два старых патча — `messenger-consent` и `max-icon-fix` (для иконки MAX визуальную проверку в Firefox делает пользователь: Firefox из этой среды не запустить). Вторым приоритетом — поднять с владельцем 🧑‍⚖️-вопросы из `docs/checklists/audit-es-trans-ru-2026-08-17.md` (договор поручения, РКН, регламент утечки, ответственный): это единственный оставшийся риск по 152-ФЗ, кода не требует. Если появится новый сайт с задачей cookie-баннера — начать с `sites/cookie-banner-universal/README.md`; если с задачей аудита — с `docs/checklists/152-fz-website-audit.md`. Фоновые хвосты — в `state.md` → «Следующие шаги»: подтвердить отзыв старого GitHub PAT, донаполнить `project-brief.md`, решить по бэкенду. **После аудита 18.08 приоритет подтверждён и не изменился:** смена пароля, затем выкладка `consent-log/` — это единственное техническое замечание с прямой нормой, всё остальное по коду закрыто и проверено. После выкладки перепроверить curl-прогоном, что POST без `agreement` отклоняется, и тестовой заявкой из каждой формы, что валидные заявки доходят. Вторым приоритетом — перечень ПДн в документах (ст. 5: заявлены дата и место рождения, адрес регистрации, образование, профессия, ИНН, которых нет ни в одной форме) и вебвизор Метрики без отдельного согласия. Полный список 🧑‍⚖️-вопросов к владельцу — в конце отчёта за 18.08.
