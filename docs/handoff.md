# 🤝 ПЕРЕДАЧА ПРОЕКТА — ЧИТАЙ ЭТО ПЕРВЫМ

> Этот файл — точка входа для **новой нейронки** (или человека), которая
> впервые открывает проект. Цель: за 5 минут понять **что это, где мы
> сейчас и куда идём** — без необходимости читать весь код.

---

## Как устроена память проекта

Весь контекст проекта живёт в четырёх файлах. У каждого — одна роль.
Читай их **в этом порядке**:

| # | Файл | Отвечает на вопрос | Как меняется |
|---|------|--------------------|--------------|
| 1 | [project-brief.md](./project-brief.md) | **Что** это за проект и **зачем** | Редко (меняется суть проекта) |
| 2 | [state.md](./state.md) | **Где мы сейчас** и **что делать дальше** | Постоянно — это «живой снимок» |
| 3 | [changelog.md](./changelog.md) | **Что уже сделали** (по датам) | Только дописывается вниз |
| 4 | [decisions.md](./decisions.md) | **Почему** так, а не иначе | Только дописывается вниз |

Плюс [../CLAUDE.md](../CLAUDE.md) — инструкции по стеку, деплою и правилам работы.

**Правило, которое держит всё это в актуальном состоянии:**
после каждого осмысленного шага мы обновляем `state.md` + дописываем
`changelog.md` и **коммитим в git**. Поэтому история проекта = история
коммитов, а текущее положение дел всегда в `state.md`. Подробнее — в
разделе «ЖУРНАЛ ПРОЕКТА И КОММИТЫ» в `CLAUDE.md`.

---

## Быстрый старт для входящей нейронки

1. Прочитай 4 файла выше по порядку.
2. Глянь последние коммиты — это лента того, что реально делалось:
   ```bash
   git log --oneline -20
   ```
3. Если нужно увидеть, что менялось в конкретном шаге:
   ```bash
   git show <hash>
   ```
4. Посмотри, какие скиллы доступны — это основной инструментарий проекта:
   ```bash
   ls .claude/skills/
   ```
   Описание каждого — в [architecture.md](./architecture.md).
5. Возьми первый пункт из раздела «Следующие шаги» в `state.md` — и продолжай.

---

## Снимок передачи

> Перед тем как передать проект дальше, обнови блок ниже (или вызови `/handoff`).
> Это TL;DR на одну страницу — самое важное, что нельзя потерять.

**Дата снимка:** 2026-08-25 (патч `consent-log/` выложен на прод, оба 🔴 по 152-ФЗ закрыты)
**В одном предложении:** Патч `sites/es-trans-ru/consent-log/`, лежавший готовым с 18.08, выложен на прод 25.08 целиком — все шесть шагов пройдены и проверены снаружи, **оба 🔴 закрыты и красных пунктов в аудите не осталось**: **ч. 1 ст. 19** — пароль SMTP сменён и вынесен в конфиг вне `public_html` (600, папка 700, недоступен по 8 проверенным путям, в исходнике обработчика пароля нет); **ч. 1 ст. 9** — согласие проверяется на сервере, прямой POST без `agreement` теперь **отклоняется** (было: 200/0 б, неотличимо от запроса с согласием), и каждое согласие пишется в таблицу `consents` вместе с текстом, который человек видел, его sha256 и версией редакции. Сквозная проверка сошлась: письмо на `kpv@es-trans.pro` с блоком «Согласие получено», редакцией `2026-08-25` и UTC-меткой 16:44:07 — и запись в базе с тем же временем до секунды; заявка бота с заполненным honeypot не попала ни в почту, ни в базу. **Заодно проверена правка документов:** восстановлены четыре реквизита оператора, снесённые недокументированным деплоем 21.08 (Химки, ИНН 5047078788, КПП 504701001, ОКПО 96323417, ОГРН 1065047062574 — ч. 4 ст. 9 п. 1), дата в обоих документах 25.08.2026. **Инцидент дня:** после первой выкладки формы ~час отдавали HTTP 500 с пустым телом — `declare(strict_types=1)` при заливке съехал на строку 3, что PHP запрещает; причина найдена только после включения логов (на Timeweb они **выключены по умолчанию**), исправлено переносом `declare` в первую строку вместе с тегом и переводом `require` PHPMailer на `__DIR__` — см. **D-005**. **Осталась одна открытая техническая задача** — правка карты на `contacts.html` (диагноз от 21.08, на прод не выложена).
**Снимок предыдущей сессии (21.08, вечер):** Диагностическая сессия без кода и без выкладок: причина неработающей карты на `contacts.html` найдена и подтверждена измерением — в `data-map-src` стоит ссылка **для перехода** на сайт Яндекс.Карт (`/maps/?…&source=constructorLink`), которую Яндекс отдаёт с `X-Frame-Options: DENY`, тогда как встраиваемый виджет (`/map-widget/v1/?…&source=constructor`) этого заголовка не имеет и во фрейме открывается; ключевая улика — заглушка на скриншоте **браузерная, а не наша**, то есть cookie-гейтинг отработал штатно и `cookie-banner.js` трогать не нужно. Готовая правка: `/maps/` → `/map-widget/v1/`, `source=constructorLink` → `source=constructor` при сохранении прод-овского ID `dad8984b…`; строка целиком — в `state.md`. **Побочная находка:** репозиторий разошёлся с продом по ID карты (`consent-gating-fix/contacts.AFTER.html` = `9d231bda…`), прод-ID проверен и валиден — сохранять надо прод-овский. Правка в репозиторий не вносилась, два вопроса остались без ответа (вносить ли фикс; синхронизировать ли ID) — **оба всё ещё открыты**.
**Снимок предыдущей сессии (20.08):** Проведён полный аудит es-trans.ru на новом табличном шаблоне `docs/checklists/TEMPLATE-pdn-audit.md` (71 пункт, 8 блоков) — отчёт `docs/checklists/audit-es-trans-ru-2026-08-20.md`, итог **3 ❌-пункта (= 2 проблемы) / 7 ⚠️ / 15 🧑‍⚖️**; найденное в этой же сессии замечание по Яндекс.Картам (не были названы в документах, хотя передают ООО «ЯНДЕКС» IP-адрес — п. 1 ч. 1 ст. 14) **закрыто владельцем в тот же день за шесть деплоев, все 12 правок внесены и проверены в 09:15:28 GMT**; по ходу исправлена собственная ошибка предыдущей редакции отчёта — Карты **не ставят ни одной cookie** (контрольный эксперимент с блокировкой хостов), все 16 принадлежат Метрике, а основание требовать упоминания — передача IP; открытым текстовым замечанием осталось одно 🟡: в разделе 6 Политики не назван 30-дневный срок уничтожения (ч. 5 ст. 21, пункт 2.13). **Оба 🔴 переходят без изменений** — скомпрометированный пароль SMTP (ч. 1 ст. 19) и отсутствие серверной проверки согласия (ч. 1 ст. 9, проверено повторно: POST без `agreement` принимается неотличимо от запроса с ним), закрываются одной выкладкой готового патча `sites/es-trans-ru/consent-log/`, начиная со смены пароля. **Внимание:** пуш в GitHub не работает (PAT протух), коммиты 18–20.08 только локальные.
**Снимок предыдущей сессии (18.08):** На проде es-trans.ru закрыт гейтинг Яндекс.Карт и отложенная загрузка Битрикс24 плюс найденный в этой же сессии дефект определения языка модалок (63/63 проверки зелёные), сверен фактический набор cookie с перечнем в Политике (владелец сразу же поправил Политику, сверено), а главная находка — согласия из трёх не-битриксовых форм **не сохранялись нигде** — закрыта готовым, но пока не выложенным патчем `sites/es-trans-ru/consent-log/`; самое срочное сейчас не код, а смена скомпрометированного пароля почты. **Итог вечернего повторного аудита 18.08 (`docs/checklists/audit-es-trans-ru-2026-08-18.md`):** всё выложенное 17–18.08 подтверждено на живом проде (cookie-блок эталонный — 12 страниц × 3 сценария, до выбора и при отказе 0 внешних хостов и 0 cookie везде; разметка форм чиста на 9 страницах; почта на своём домене; перечень действий в Согласии полный; язык согласия совпадает с языком страницы), а незакрытых технических замечаний осталось три — **нет серверной проверки согласия** (🔴 ч. 1 ст. 9: прямой POST на `sendmail.php` без поля `agreement` принимается неотличимо от запроса с ним — 200/0 байт/0.068 с, как и на пустой POST и GET; через браузер блокировка работает, но обходится отключением JS), скомпрометированный пароль SMTP (🔴 ч. 1 ст. 19) и перечень ПДн в документах шире реально собираемого формами (🟡 ст. 5); первые два закрываются одной выкладкой уже готового патча `consent-log/`, начиная со смены пароля.
**Что работает прямо сейчас:** 35 скиллов в `.claude/skills/` — правка вёрстки, дизайн и темы (`frontend-design`, `theme-factory`, `ui-ux-pro-max`) и процесс (`superpowers`, `/log`, `/end`). Браузерная автоматизация (`playwright-skill`, `webapp-testing`) проверена и многократно использована на живом проде. На `https://es-trans.ru/` работает end-to-end: cookie-баннер (до выбора и при отказе — **0 cookie и 0 внешних хостов**, подтверждено 2026-08-18 на 12 страницах), гейтинг Яндекс.Метрики, **Яндекс.Карт** (заглушка с кнопкой «Показать карту» вместо iframe) и **отложенная загрузка формы Битрикс24**, точка отзыва согласия «Настройки cookie» в футере, локализация всех наших модалок по цепочке `localStorage['language'] || navigator.language.slice(0,2) || 'ru'` со списком `['ru','en','cn']` — как у самого сайта. Прод-проверка `consent-gating-fix/verify.cjs`: **63/63**, отдельно модалка мессенджера 9/9 по локалям. Согласие во всех трёх формах отображается корректно на всех страницах. Есть переиспользуемый чек-лист аудита по 152-ФЗ ред. 2026 — `docs/checklists/152-fz-website-audit.md` (🔍 проверяется технически / 🧑‍⚖️ нужен ответ владельца), примеры применения — `audit-es-trans-ru-2026-08-17.md` и `audit-cookie-vs-policy-2026-08-18.md`. Универсальный шаблон баннера — `sites/cookie-banner-universal/`. Репозиторий на GitHub: `github.com/krutko77/website-customizer`, ветка `main`.
**Что в процессе / недоделано:** 🆕 **Карта на `contacts.html` не отображается — единственная открытая техническая задача.** Причина найдена 21.08, правка не внесена: в `data-map-src` заменить `/maps/` → `/map-widget/v1/` и `source=constructorLink` → `source=constructor`, **сохранив прод-овский ID** `constructor%3Adad8984b336259f49920289107cb16f22c256c86243b42100f2d8bb2050ae10d` (готовая строка — в `state.md`). Правку вносит владелец в Gulp-проекте; `contacts.html` одна, полная пересборка не нужна. После деплоя проверить Playwright-ом, что карта рисуется после «Принимаю» и по кнопке «Показать карту», **и что до выбора и при отказе по-прежнему 0 запросов к Яндексу** — гейтинг сейчас исправен, его легко сломать заодно. Отдельно решить, синхронизировать ли ID карты в `sites/es-trans-ru/consent-gating-fix/contacts.AFTER.html` (там устаревший `9d231bda…`). **Хвосты по свежей выкладке `consent-log/` (не блокеры):** живые формы на сайте ещё не отправлялись — проверка шла curl-ом по обработчику напрямую, вёрстка с honeypot проверена отдельно (21/21), но сквозной путь «форма на странице → письмо → база» не пройден; журнал согласий **не включён в бэкап БД**, а это единственное доказательство по ч. 1 ст. 9; в таблице лежит одна тестовая запись (id 1, `ПРОВЕРКА-БД 25.08 не отвечать`, `+70000000000`) — удалить, если нужен чистый журнал; новый пароль почты структурно повторяет старый и потому угадываем — при случае сменить на несвязанный. **Текстовое замечание:** 🟡 в разделе 6 Политики не назван 30-дневный срок уничтожения (ч. 5 ст. 21) — готовая формулировка в отчёте аудита 20.08, проблема 7. **Два старых патча без обратной связи с 2026-08-13:** `sites/es-trans-ru/messenger-consent/` и `sites/es-trans-ru/max-icon-fix/`. **Организационный хвост по 152-ФЗ (кода не требует):** договор поручения на обработку (ч. 3 ст. 6) с Яндексом, Битрикс24 и Timeweb + сверка реквизитов по свежей выписке ЕГРЮЛ; уведомление в РКН и запись в реестре операторов; регламент реагирования на утечку (ст. 21); ответственный за обработку ПДн; рабочий ли ящик `info@es-trans.pro`; перечень ПДн в Политике шире реально собираемого формами (ст. 5). Общие хвосты: `project-brief.md` заполнен частично, стек не выбран, отзыв старого GitHub PAT `ghp_...ye1EkTdP` не подтверждён.
**Где живёт прод (URL / SERVER_ID):** Не задеплоен (в рамках этого репозитория). Целевой сайт клиента — https://es-trans.ru/ (клиент сам держит исходники в отдельном Gulp-проекте на своём ноутбуке, вне доступа этой среды, и сам выкладывает правки — у нас нет прямого доступа ни к файлам, ни к серверу). VPS: 201.51.4.183, следующий свободный порт был 3004 (сверить перед использованием). `deploy-vps.sh` применим только если появится серверная часть — сейчас он ссылается на несуществующий `src/server.js`.
**Главные подводные камни:** **Логи на Timeweb выключены по умолчанию** — `access_log` и `error_log` включаются тумблерами в панели → Логи по домену, файл появляется в `~/<домен>/`. Без них HTTP 500 непрозрачна (тело пустое, в `access_log` только код ответа): 25.08 на это ушло три итерации гаданий против одной строки лога. Включать **до** выкладки. **Файлы для клиента заливать файлом, а не вставкой в редактор:** конструкции, чувствительные к позиции в файле (`declare(strict_types=1)`, BOM, пустая строка перед `<?php`), ломаются при копировании через буфер — именно так формы легли на час 25.08 (D-005). **Относительные `require` в патчах не использовать** — целевое окружение (Apache/FPM, рабочая директория) заранее неизвестно, только `__DIR__`. **Тестировать на версии PHP клиента:** на es-trans.ru **PHP 7.4**, локальный стенд 8.2. **В панели Timeweb две записи `es-trans.ru`** — «Старый сайт (Европа)» с доменами на PHP 7.4 (живой сайт) и вторая без доменов на PHP 8.2. **При работе по скриншотам проверять структуру по пунктам, а не по факту наличия объекта** — 25.08 таблица `consents` была объявлена созданной по её виду в дереве, а в ней была одна колонка. **Ссылки Яндекс.Карт бывают двух похожих видов, и только одна работает во фрейме:** «Ссылка на карту» (`/maps/?…&source=constructorLink`) отдаётся с `X-Frame-Options: DENY` и в iframe не открывается, для встраивания нужен «Код для вставки» (`/map-widget/v1/?…&source=constructor`) — именно на этом карта на «Контактах» и сломалась (21.08); симптом обманчив (браузер пишет «Сайт yandex.by не позволяет установить соединение», что выглядит как проблема сети или региона), а из РФ-независимой среды редирект не воспроизводится — проверять `X-Frame-Options`, а не код ответа. **Заглушка на месте карты бывает двух природ** — наша (с кнопкой «Показать карту», от cookie-гейтинга) и браузерная (серый блок с текстом ошибки); спутать их значит чинить не тот код. **Отдельного `js/cookie-banner.js` на проде НЕТ** — баннер минифицирован внутрь `js/app.min.js`, README ранних патчей (`cookie-banner/`, `cookie-banner-i18n-fix/`) утверждают обратное и это неверно; актуальный исходник, совпадающий с продом, — `sites/es-trans-ru/consent-gating-fix/cookie-banner.js`, а `sites/es-trans-ru/cookie-banner/cookie-banner.js` помечен устаревшим. После каждой выкладки проверять, что в бандле **одна** копия строки `es-trans-cookie-consent`, иначе на сайте будет два баннера. **Попапы с формами и футер продублированы в разметке КАЖДОЙ страницы es-trans.ru** — общего include нет, поэтому правка одного шаблона без полной пересборки расходится не на весь сайт (именно так первая выкладка правки форм 2026-08-17 попала только на одну страницу из шести). При правках форм/футера проверять все страницы: сравнить `last-modified` и искомый фрагмент разметки по списку. **Любой динамически созданный элемент на этом сайте не переводится** — функция `je()` в `js/app.min.js` отрабатывает один раз при загрузке страницы и при смене `#languageSelect`, а элемент создаётся позже; наступили уже трижды (messenger-consent, cookie-баннер), лечение всегда одно — свой словарь `TRANSLATIONS` + чтение `localStorage['language']`. **Канонические файлы в `sites/es-trans-ru/` могут отставать от прода** (так и обнаружился `cookie-banner/cookie-banner.js`) — перед правкой сверяться с реальным `app.min.js` с прода, а не доверять репозиторию; `grep` по нему отваливается по таймауту, куски тянуть точечно через python3. Формы живут в скрытых попапах — Playwright не кликнет напрямую («outside of the viewport»), нужно через `page.evaluate` форсировать `display/visibility/opacity` у родителей, иначе получаются ложные ❌. Корень проекта в этой среде — `/home/my_workspace/website-customizer` (раньше был `/data/vibe-students/krutko77/website-customizer`; захардкоженные пути в `.claude/settings.json` уже поправлены 2026-08-17, но могут всплыть ещё где-то в конфигах). Реальные файлы сайта клиента недоступны для прямого редактирования — все правки готовятся как точный текстовый патч в чате и передаются клиенту, который сам вносит их и пересобирает через Gulp; канонические файлы-эталоны в `sites/es-trans-ru/` (`cookie-banner/`, `bitrix24-form/`) нужно вручную синхронизировать с тем, что реально ушло клиенту — и это ОТДЕЛЬНАЯ копия логики от `sites/cookie-banner-universal/` (шаблон для будущих сайтов), они не связаны импортами. При любом гейтинге трекера — оборачивать нужно ВСЮ загрузку скрипта-счётчика, не только вызов инициализации (см. принцип №1 в `sites/cookie-banner-universal/README.md`) — на этом уже дважды спотыкались в этом проекте. Эта VS Code-сессия подключена через локальный shim-прокси (`ANTHROPIC_BASE_URL=http://172.30.0.1:8190`), не напрямую к аккаунту claude.ai — `/status`/Usage не показывают реальные лимиты подписки. `.env.example` НЕ в `.gitignore` (в отличие от `.env`) — секреты туда класть нельзя. Старый GitHub PAT `ghp_...ye1EkTdP` заменён новым — пользователь должен подтвердить отзыв старого на GitHub. Прямые fetch к github.com заблокированы — работать через `curl`/`git` (обычные внешние сайты, включая es-trans.ru, через `curl` доступны без ограничений). Запись в `.claude/commands/` только через python3.
**Первое, что стоит сделать новому исполнителю:** Спросить про **карту на `contacts.html`** — это единственная открытая техническая задача. Внесена ли правка `data-map-src` (`/maps/` → `/map-widget/v1/`, `source=constructorLink` → `source=constructor`, ID прод-овский) и задеплоена ли; если да — прогнать Playwright и убедиться, что карта рисуется после согласия, а до выбора и при отказе запросов к Яндексу по-прежнему ноль; если нет — выдать готовую строку из `state.md`. Заодно решить, синхронизировать ли ID карты в `consent-gating-fix/contacts.AFTER.html`. **Про пароль и выкладку `consent-log/` спрашивать больше не нужно — закрыто 25.08 и проверено снаружи.** Стоит лишь попросить пользователя прогнать по одной живой заявке из каждой из трёх форм через браузер (сквозной путь «форма → письмо → база» ещё не проверялся) и напомнить про бэкап таблицы `consents` — это единственное доказательство согласия по ч. 1 ст. 9. Заодно спросить про два старых патча — `messenger-consent` и `max-icon-fix` (для иконки MAX визуальную проверку в Firefox делает пользователь: Firefox из этой среды не запустить). Вторым приоритетом — текстовое 🟡: 30-дневный срок уничтожения в разделе 6 Политики (ч. 5 ст. 21), готовая формулировка в отчёте за 20.08, проблема 7. Третьим — поднять с владельцем 15 🧑‍⚖️-вопросов без ответа с 17.08 (договор поручения, РКН, регламент утечки, ответственный); список в конце `audit-es-trans-ru-2026-08-20.md`: это единственный оставшийся риск по 152-ФЗ, кода не требует. Если появится новый сайт с задачей cookie-баннера — начать с `sites/cookie-banner-universal/README.md`; если с задачей аудита — с `docs/checklists/152-fz-website-audit.md`. Фоновые хвосты — в `state.md` → «Следующие шаги»: подтвердить отзыв старого GitHub PAT, донаполнить `project-brief.md`, решить по бэкенду.
