# Последняя сессия — 2026-08-11

## Кто работал
не указано

## Что было сделано
- Найдены и отобраны скиллы на GitHub под задачу «правки на сайты»
  (поиск через `curl`/`git` — прямые запросы к github.com в окружении
  заблокированы)
- Установлено локально в `.claude/skills/` 5 скиллов:
  - `webapp-testing`, `frontend-design`, `theme-factory`, `skill-creator`
    — официальный `anthropics/skills` (167 761★, обновлён 2026-08-07)
  - `playwright-skill` — `lackeyjb/playwright-skill` (3013★, обновлён 2026-08-11)
- Сторонний `playwright-skill` проверен перед установкой: `execSync`
  вызывается только для установки самого Playwright, посторонних сетевых
  вызовов нет
- Удалён весь Б24-специфичный код и документация: папка `src/` целиком,
  Б24-переменные в `.env.example`, express/dotenv и скрипты сервера в
  `package.json`, Б24-инструкции в конце `deploy-vps.sh`
- Переписаны `docs/architecture.md`, `project-brief.md`, `state.md`,
  снимок в `handoff.md`
- Скачаны `playwright` и Chromium; при проверке запуском обнаружено, что
  браузер не стартует — блокер зафиксирован в документации
- Проект впервые взят под git через `scripts/snapshot.sh`

## Ключевые решения
- Скиллы ставим **локально** в проект, не глобально — чтобы не засорять
  другие проекты (D-002)
- Критерий отбора скиллов: официальный источник либо ≥3000★ + свежее
  обновление + ручной аудит кода на посторонние сетевые вызовы и `exec`.
  По этому критерию отклонены `willmarple/playwright-skill` (7★) и
  `akaihola/playwright-py-skill` (3★) — мало звёзд, давно без обновлений
- Б24-код удалён полностью, а не заархивирован — история есть в git,
  мёртвый код вводит в заблуждение следующего исполнителя
- `deploy-vps.sh` сохранён: он универсальный (PM2 + nginx + SSL),
  Б24-специфичны были только две строки в конце

## На чём остановились
Инструментарий установлен и проект очищен от Б24; последним действием
сделан снимок в git — ждём конкретику по сайтам и задачам.

## Что делать следующему
1. Решить вопрос с системными библиотеками для Chromium — без этого
   браузерная автоматизация (`playwright-skill`, `webapp-testing`)
   не работает. Нужен админ: `npx playwright install-deps chromium` от root
2. Заполнить `project-brief.md` — клиент, сайты, типовые задачи
3. Определить стек — `package.json` сейчас без зависимостей
4. Если нужен пуш на GitHub — настроить remote (`git remote add origin …`),
   сейчас его нет

## Подводные камни
- **Chromium не стартует в этом окружении.** `playwright` и Chromium
  скачаны успешно, но бинарнику
  `~/.cache/ms-playwright/chromium_headless_shell-1234/.../chrome-headless-shell`
  не хватает 20 системных библиотек (`libglib-2.0.so.0`, `libnss3.so`,
  `libX11.so.6`, `libasound.so.2` и др.).
  Проверка: `ldd <бинарник> | grep "not found"`.
  Лечится `npx playwright install-deps chromium` от root — но `sudo` в
  окружении не установлен
- `deploy-vps.sh` запускает через PM2 `src/server.js`, которого больше
  нет — перед деплоем указать реальную точку входа (пометка в шапке скрипта)
- `CLAUDE.md` пустой (0 байт). Судя по старым докам, там был реестр портов
  и протокол запуска проекта — они были потеряны ДО этой сессии, не в ней
- Прямые fetch-запросы к github.com заблокированы; GitHub доступен через
  `curl`/`git` в шелле
- Бэкап удалённого Б24-кода лежит в `/tmp/_backup_docs` и `/tmp/_backup_src` —
  `/tmp` чистится при перезагрузке, если нужен надолго — перенести
- Запись в `.claude/commands/` требует python3, а не bash `cat` или Write
- `gh` CLI не установлен — пуш идёт через `~/.git-credentials` (store-helper)
