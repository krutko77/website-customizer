# Последняя сессия — 2026-08-11

## Кто работал
не указано

## Что было сделано
- Проверена браузерная автоматизация: headless Chromium запускается и
  грузит страницы напрямую через `playwright-skill`, без доустановки
  системных библиотек и без root. Прошлый блокер (не хватает 20
  библиотек, нет sudo) оказался ложным — `install-deps` падает без root,
  но фактически не требуется
- Обновлены `docs/handoff.md` и `docs/decisions.md` (D-003) с этой находкой
- Заполнен `CLAUDE.md` (был пустым 0 байт) — стек/статус, шпаргалка по
  скиллам, правила ведения docs-журнала, деплой, ограничения окружения
- Подключён GitHub-репозиторий: `git remote add origin
  https://github.com/krutko77/website-customizer.git`, ветка
  переименована `master` → `main`, история запушена (`git push -u`)
- **Инцидент с токеном:** пользователь вставил реальный GitHub PAT в
  `.env.example` (этот файл, в отличие от `.env`, не в `.gitignore` и
  обычно коммитится). Токен использован один раз для push через URL с
  embedded-credentials, затем: `.env.example` возвращён к чистому
  шаблону **до** коммита (в git-историю не попал), временная запись
  токена в `.git/config` (branch remote-tracking строка) удалена

## Ключевые решения
- D-003: браузерная автоматизация признана рабочей без доп. настройки —
  см. `docs/decisions.md`

## На чём остановились
Push на GitHub выполнен успешно; последнее незавершённое действие —
восстановление строки `remote = origin` под `[branch "main"]` в
`.git/config` (upstream-трекинг настроен частично, GitHub-предложенные
команды `git push`/`git pull` без аргументов могут потребовать `-u`).
Пользователь прервал этот конкретный edit — решение оставлено на
усмотрение следующего исполнителя.

## Что делать следующему
1. **Отозвать GitHub PAT `ghp_...ye1EkTdP`** (аккаунт `vibe-krutko77`,
   github.com/settings/tokens) и выпустить новый — старый токен был
   виден в открытом виде (модели и в рабочей копии), считать
   скомпрометированным из осторожности, хотя в git-историю не попал
2. Решить, восстанавливать ли `remote = origin` в `[branch "main"]`
   `.git/config` — иначе `git push`/`pull` без флагов может не найти
   upstream
3. Заполнить `project-brief.md` — клиент, сайты, типовые задачи
4. Определить стек — `package.json` сейчас без зависимостей

## Подводные камни
- **`.env.example` — не гитигнорится, в отличие от `.env`.** Секреты
  туда класть нельзя, они уйдут в публичный репозиторий при следующем
  коммите. Настоящие секреты — только в `.env`
- `deploy-vps.sh` запускает через PM2 `src/server.js`, которого больше
  нет — перед деплоем указать реальную точку входа (пометка в шапке скрипта)
- Прямые fetch-запросы к github.com заблокированы; GitHub доступен через
  `curl`/`git`/HTTPS с токеном в шелле
- Push через HTTPS требует credentials на каждый раз — ни `gh` CLI, ни
  SSH-ключ, ни credential helper в окружении не настроены
- Запись в `.claude/commands/` требует python3, а не bash `cat` или Write
