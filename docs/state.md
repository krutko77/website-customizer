# 📍 Состояние проекта — где мы сейчас

> **Живой снимок.** Этот файл всегда отражает текущее положение дел.
> Его не дописывают вниз, а **перезаписывают** — здесь только «сейчас»,
> вся история — в [changelog.md](./changelog.md).
> Обновляй после каждого осмысленного шага и **коммить**.

**Последнее обновление:** 2026-08-17
**Фаза:** es-trans.ru — cookie-баннер, Bitrix24-гейтинг и фикс cookie `bh` (Метрика) полностью готовы, задеплоены и проверены на проде; из этого опыта собран универсальный шаблон для будущих сайтов. Два патча (messenger-consent для Telegram/MAX и фикс чёрной иконки MAX в Firefox) переданы клиенту и ждут применения/деплоя — обратной связи с 2026-08-13 не поступало

---

## Где мы сейчас

**Сессия 2026-08-17** была короткой и без правок по сайту: восстановлен
контекст из `docs/`, пользователю выдан статус-обзор, после чего он
попросил встать на паузу. Обратной связи от клиента по двум переданным
патчам (messenger-consent, max-icon-fix) не поступало — обе задачи в том
же состоянии «ждём деплоя», что и 2026-08-13. Попутно приведён в порядок
хвост в рабочей копии: в `.claude/settings.json` абсолютные пути вели на
несуществующий `/data/vibe-students/krutko77/website-customizer` (среда
переехала на `/home/my_workspace/website-customizer`) — заменены на
актуальные; удалён временный артефакт
`.claude/skills/playwright-skill/.temp-execution-*.js`; закоммичены
`hooks.json` в корне репозитория и бит +x на
`.claude/skills/superpowers-main/hooks/session-start` (появились не в
этой сессии, origin не установлен — `hooks.json` ссылается на
`${CLAUDE_PLUGIN_ROOT}/hooks/session-start`, то есть его место скорее
внутри скилла `superpowers-main`, а не в корне; если мешает — можно
удалить).

Cookie-баннер для `es-trans.ru` **выложен на прод и работает полностью**
(история фикса — см. записи от 2026-08-11/12 в `changelog.md`). Клиент
сам держит исходники сайта в отдельном Gulp-проекте на своём ноутбуке
(`es_trans_china`, вне доступа этой среды) — мы не редактируем файлы
напрямую, а описываем точные патчи текстом в чате, клиент вносит их сам
и пересобирает через Gulp.

В этой сессии (продолжение 2026-08-12, после закрытия мобильного бага):
1) по запросу пользователя проведён аудит сайта на соответствие 152-ФЗ
«О персональных данных» — баннер, формы и документы политики признаны
в целом соответствующими на первый взгляд, отмечены пункты для сверки с
клиентом (даты в документах похожи на шаблонные, несовпадение домена
почты `info@es-trans.pro` vs `es-trans.ru`, неясен статус уведомления
Роскомнадзора, слишком общий список типов персданных в политике); 2) по
скриншоту DevTools с cookie `_ga`/`_ga_*` заподозрено, что на сайте
работает незадекларированная Google Analytics — Playwright-проверкой
живого прода (`services-customs.html`, все сетевые хосты) гипотеза
опровергнута: своей GA у сайта нет, `_ga`/`_ga_*` ставит **встроенный
виджет формы заявки Bitrix24** (`data-b24-form`, `cdn-ru.bitrix24.ru` /
`es-trans.bitrix24.ru`) — он использует GA для собственной аналитики;
3) отдельным Playwright-прогоном подтверждено, что сам виджет Bitrix24
(включая его аналитический вызов `crm.site.form.handleAnalytics`)
стартует **безусловно при загрузке страницы**, до любого выбора в
cookie-баннере — тот же класс нарушения, что был раньше у Метрики;
4) с пользователем согласован фикс: полностью отложить загрузку скрипта
формы (`loader_16.js`) до того, как посетитель сделает **любой** выбор
в баннере (Принимаю ИЛИ Отказаться — форма нужна для приёма заявок, не
только аналитики, поэтому доступна при обоих исходах, в отличие от
Метрики), с текстом-заглушкой на месте формы до выбора; 5) канонический
файл `sites/es-trans-ru/cookie-banner/cookie-banner.js` обновлён под эту
логику (`initBitrixFormIfAllowed()`); 6) создана новая папка
`sites/es-trans-ru/bitrix24-form/README.md` с полным патчем (HTML
заглушка, CSS, JS-логика, чек-лист проверки) — по аналогии с
`cookie-banner/README.md`; 7) клиент применил патч и задеплоил, но при
первой проверке в инкогнито обнаружил баг: после «Принимаю» куки
появлялись, заглушка пропадала, но вместо формы был пустой попап —
диагностирован через Playwright: скрипт `loader_16.js` вставлялся рядом
с первым `<script>` страницы (обычно в `<head>`), а с
`data-skip-moving="true"` виджет Bitrix24 рендерит себя именно там, где
физически лежит его `<script>` — форма отрисовывалась в `<head>`,
невидимо. Исправлено: скрипт теперь вставляется `mount.appendChild(s)`
внутрь `#b24-form-mount` — подтверждено тестом (форма, 6 полей, рендерится
в placeholder); 8) по запросу пользователя ещё раз создан готовый к
вставке в Gulp-сборку файл `sites/es-trans-ru/bitrix24-form/cookie-banner-b24.js`
с этим фиксом; 9) клиент пересобрал и задеплоил — финальная
Playwright-проверка живого прода (`es-trans.ru`, изолированные контексты,
сценарии accept/decline) подтвердила: **нет ни одного запроса или cookie,
связанных с Google/GA/GTM/DoubleClick/gstatic** ни до, ни после согласия
ни в одном сценарии; Метрика (`_ym_*` и т.д.) появляется только после
«Принимаю»; форма Bitrix24 (6 полей) корректно рендерится в попапе и
доступна после ЛЮБОГО выбора (accept и decline); баннер не показывается
повторно после reload. Фича полностью закрыта end-to-end на проде.

GitHub PAT ротирован (сессия 2026-08-11): старый (`ghp_...ye1EkTdP`)
заменён новым, пуш работает штатно, upstream-трекинг `main` →
`origin/main` в порядке — отзыв старого PAT на стороне GitHub всё ещё не
подтверждён пользователем.

## Сейчас в работе

Ничего не делается прямо сейчас — обе активные задачи на стороне
клиента, ждём деплоя и обратной связи. Ниже — их состояние на момент
паузы.

Патч для es-trans.ru — уведомление о переходе в Telegram/MAX
(вариант 3, согласованный с пользователем) — готов
(`sites/es-trans-ru/messenger-consent/`: JS + CSS + README) и передан
пользователю в чате. Клиент уже применил первую версию и задеплоил, но
по ходу проверки на проде вскрылись два бага, оба найдены по
скриншотам пользователя и исправлены в этой же сессии:

1. **Вторая ссылка в модалке не рендерилась** («Согласием на обработку
   данных» отображалось как обычный текст) — причина оказалась в
   хрупкой сборке `innerHTML`-строкой с двумя одинаковыми
   `class="messenger-consent__link"` подряд. Фикс: модалка теперь
   строится через DOM-методы (`createElement`/`appendChild`).
   (Первая версия объяснения причины была ошибочной — предполагался
   несуществующий скрипт-сканер локализации; пользователь прислал
   реальный `script.js` сайта и поправил: `data-lang` на сайте всегда
   прописывается вручную в коде, как в `cookie-banner.js`, никакого
   автосканирования нет.)
2. **Текст модалки не переключался вместе с языком сайта** (сайт на
   английском, модалка всё ещё на русском) — причина: функция перевода
   сайта `je()` (в `js/app.min.js`) применяется только один раз при
   загрузке страницы / смене `#languageSelect`, а модалка создаётся
   позже, в момент клика, и `je()` её уже не видит. Фикс: модалка
   переводит себя сама — свой словарь `TRANSLATIONS` (синхронизирован
   со значениями, которые сайт сам сгенерировал в `app.min.js` для
   `messenger-consent-1`…`-8`) + чтение `localStorage['language']`, того
   же ключа, что использует переключатель языка на сайте.

Оба фикса подтверждены Playwright-проверками на живом проде. Также
обнаружено (по присланному `script.js`): в реальном файле на сервере
клиента накопилось несколько дублирующихся копий блока
messenger-consent от последовательных попыток патча — в README
добавлена инструкция удалить все старые копии и оставить только
актуальную. Ждём, когда клиент обновит `messenger-consent.js` на
сервере финальной версией и передеплоит — после этого нужна ещё одна
Playwright-проверка по полному чек-листу из README (включая оба
фикса и языковое переключение).

Отдельно от messenger-consent: пользователь сообщил, что иконка MAX в
шапке сайта в Firefox отображается сплошным чёрным пятном. Причина —
хрупкая конструкция символа `#max` в `img/icons/icons.svg` (вложенный
`<svg>` с PNG-картинкой, обрезанной через `<clipPath>`+`<use>`),
внутренние id-ссылки которой Firefox не резолвит при вызове символа
через внешний спрайтовый `<use>` (Chrome резолвит, поэтому там иконка
выглядит нормально). Подготовлен патч
`sites/es-trans-ru/max-icon-fix/` — готовый PNG (получен рендером
того же символа в Chromium, где clipPath работает верно) + замена
`<svg><use></use></svg>` на `<img>` в разметке, CSS менять не нужно.
Передано пользователю, ждём применения на сервере и деплоя.

Cookie-баннер, Bitrix24-гейтинг и фикс пробела с cookie `bh` (Метрика)
— все три фичи проверены end-to-end на живом проде, без изменений с
прошлой сессии. Универсальный шаблон cookie-баннера в
`sites/cookie-banner-universal/` — без изменений.

## Следующие шаги

- [ ] Спросить у пользователя, применил ли клиент финальную версию
      `sites/es-trans-ru/messenger-consent/` (и вычистил ли дубли блока
      в `script.js`) — если да, прогнать Playwright-проверку живого прода
      по чек-листу из README: модалка на первый клик, «Отмена»/«Перейти»,
      не показывается повторно, независимо для Telegram/MAX, работает на
      разных страницах, текст переключается вместе с языком сайта
- [ ] Так же проверить патч `sites/es-trans-ru/max-icon-fix/` — после
      деплоя убедиться, что в шапке отдаётся `<img>` с PNG (а не
      `<svg><use>`), сама картинка доступна по URL и размер/резкость
      совпадают с остальными иконками. Firefox из этой среды не
      запустить, поэтому визуальную проверку именно в Firefox делает
      пользователь; Playwright-ом проверяем разметку и загрузку файла
- [ ] Подтвердить на GitHub (Settings → Developer settings → Personal
      access tokens), что старый PAT `ghp_...ye1EkTdP` реально отозван
- [ ] Заполнить остальные разделы `project-brief.md` (доступы/ключи,
      дизайн) — сейчас заполнена только часть про клиента и es-trans.ru
- [ ] Определить, нужен ли бэкенд/сервер — от этого зависит `package.json`
      и применимость `deploy-vps.sh`
- [ ] Если появятся новые правки на стороне клиента — держать в уме, что
      реальные исходники живут вне этой среды (Gulp-проект на ноутбуке
      клиента), патчи готовятся текстом и передаются в чате

## Открытые вопросы / блокеры

- `package.json` сейчас пустая заготовка без зависимостей. Стек зависит от
  того, какого рода правки будут вноситься — уточнить.
- Эта среда (VS Code + Claude Code) подключена через локальный
  shim-прокси (`ANTHROPIC_BASE_URL=http://172.30.0.1:8190`), не напрямую
  к аккаунту claude.ai — панель Usage и `/status` не показывают реальные
  данные о лимитах/подписке; похоже на учебную/тренировочную песочницу.
- У нас нет прямого доступа к файлам сайта клиента (Gulp-проект на его
  ноутбуке) — весь дальнейший фронтенд-репаунд идёт через текстовые
  патчи и обратную связь по скриншотам, а не через прямое редактирование.

## Карта проекта (опорные точки)

*(Где что лежит — чтобы новой нейронке не искать. Заполняется по ходу.)*

| Что | Где |
|-----|-----|
| Установленные скиллы | `.claude/skills/` |
| Описание скиллов и процесса | `docs/architecture.md` |
| Деплой на VPS | `deploy-vps.sh` |
| Снимок в git | `scripts/snapshot.sh` |
| Репозиторий GitHub | `github.com/krutko77/website-customizer` (main) |
| Прод | URL: — · SERVER_ID: — |
| Универсальный шаблон cookie-баннера | `sites/cookie-banner-universal/` |
